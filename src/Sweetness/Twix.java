package Sweetness;

public class Twix extends AbstractSweetness {
    public Twix(String name, double weight, double price, String description){
        super.setName(name);
        super.setWeight(weight);
        super.setPrice(price);
        super.setDescription(description);
    }
}
